name: test_project

entryPoint: main.sc

botEngine: v2
language: ru

tests:
  include:
    - telegram.xml
    - whatsApp.xml
    # - widget.xml

injector:
  isFailureNow: false
  sendUserDataUrl: "https://test.url.ru/v0/"

nlp:
  intentNoMatchThresholds:
    phrases: 0.2
    patterns: 0.2